# AdGuard Home Lab Setup

Dockerized AdGuard Home with DNSSEC and DoH.

## Setup
1. Start container:
   docker-compose up -d
2. Access dashboard:
   http://<your-ip>:3000
3. Configure router DNS to point to this server.
